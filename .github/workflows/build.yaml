name: 🛠 Android CI / Build & Release APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    tags:    [ 'v*.*.*' ]
  pull_request:
    branches: [ main ]

permissions:
  contents: write   # allow creating/updating Releases & uploading assets
  actions:  read    # allow pulling marketplace actions

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build_and_release:
    runs-on: ubuntu-latest

    steps:
      - name: 📂 Checkout code
        uses: actions/checkout@v3

      - name: 🚀 Cache pub deps
        uses: actions/cache@v3
        with:
          path: ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.yaml') }}
          restore-keys: ${{ runner.os }}-pub-

      - name: ☕️ Setup Java (Temurin 17)
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: '17'

      - name: 🦋 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: 📥 Get dependencies
        run: flutter pub get

      - name: 🔥 Install Firebase CLI
        run: npm install -g firebase-tools
        
      # Method 1: Using Firebase CLI to download the config file
      - name: 📥 Download Firebase Configuration
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
        run: |
          firebase use $FIREBASE_PROJECT_ID --token $FIREBASE_TOKEN
          firebase apps:sdkconfig android $FIREBASE_APP_ID --token $FIREBASE_TOKEN > android/app/google-services.json

      - name: ✅ Run tests
        run: flutter test --coverage

      - name: 🏗️ Build APKs
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
        run: flutter build apk --release --split-per-abi

      - name: 📦 Prepare artifacts
        uses: actions/upload-artifact@v4
        with:
          name: latest-apk-build
          path: build/app/outputs/flutter-apk/*.apk